# Demo Template

Online demo: TODO

## What It Shows

Describe the SpreadJS feature or real-world scenario.

## Files

- `package.json`: local runtime dependencies.
- `index.html`: browser entry.
- `src/app.js`: demo logic.
- `src/styles.css`: demo styles.
- `assets/`: local assets used by this demo.

## Run

```bash
npm install
```

Serve the repository root or the `samples/` directory with a local static server, then open this demo's `index.html`.

## Implementation Notes

Use `@grapecity-software/*` packages from `node_modules` and reuse `samples/shared/systemjs.config.js`. Do not add CDN runtime dependencies.

List key SpreadJS APIs, browser requirements, assets, and known limitations.
