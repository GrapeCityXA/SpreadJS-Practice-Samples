# Samples

This folder organizes local SpreadJS practice examples by the same categories used by the source demo menu.

Each top-level folder is a source demo category, and each child folder is a single demo:

```text
samples/<category-slug>/<demo-slug>/
```

Some demo folders may start with only an online demo link while the local code is being organized.
